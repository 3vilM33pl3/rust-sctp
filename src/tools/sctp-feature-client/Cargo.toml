[package]
name = "sctp-feature-client"
version = "0.1.0"
edition = "2021"

[workspace]

[dependencies]
bytes = "1"
crc = "3.4"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
sctp-proto = { path = "../../../library/vendor-std/sctp-proto" }
ureq = { version = "2.2", default-features = false, features = ["json"] }
